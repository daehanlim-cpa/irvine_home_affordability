# Local tooling for the verification gate. Runtime dependencies for Snowflake
# stored procedures are declared in the procedures themselves (PACKAGES = ...),
# not here.
sqlfluff==4.3.0
PyYAML>=6.0
snowflake-connector-python>=3.12.0
snowflake-snowpark-python>=1.20.0
pytest>=8.0
